const upperCase = (string) => string.toUpperCase();

const firstLetter = (string) => string[0];

const stringsConcat = (str1, str2) => `${str1}${str2}`;

module.exports = { upperCase, firstLetter, stringsConcat };